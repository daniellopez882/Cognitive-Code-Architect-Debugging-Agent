[pytest]
testpaths = tests
# The suite was written against the src/ layout: it imports `tools.code_analysis`
# and `agents.graph`, which only resolve with src/ on the path. Without this,
# every test errored at collection and the suite had never run.
pythonpath = src
asyncio_mode = auto
addopts = -q --strict-markers --tb=short
