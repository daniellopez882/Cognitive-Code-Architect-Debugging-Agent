# Runtime dependencies: what src/ imports, plus pylint, which run_pylint
# invokes as a subprocess.
#
# The previous file pinned about thirty packages -- semgrep, py-spy, safety,
# tree-sitter, reportlab, PyGithub, python-gitlab, black, flake8, mypy,
# memory-profiler, jinja2, tenacity, astroid, coverage -- none of which the
# code imports. A test fails if a declared dependency is not used.
langgraph>=0.2,<2.0
langchain-core>=0.3,<2.0
langchain-google-genai>=2.0,<4.0
gitpython>=3.1,<4.0
radon>=6.0,<7.0
pylint>=3.3,<4.0
rich>=13.9,<15.0
click>=8.1,<9.0
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0
